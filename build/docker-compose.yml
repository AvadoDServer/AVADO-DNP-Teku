version: '3.4'
services:
  teku-gnosis.avado.dnp.dappnode.eth:
    image: 'teku-gnosis.avado.dnp.dappnode.eth:0.0.7'
    build:
      context: ./build
      args:
        TEKU_VERSION: 23.4.0
    volumes:
      - 'data:/data'
    environment:
      - JAVA_OPTS=-Xmx4g
      - EXTRA_OPTS=
volumes:
  data: {}
