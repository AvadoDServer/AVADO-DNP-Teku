version: '3.4'
services:
  teku-prater.avado.dnp.dappnode.eth:
    image: 'teku-prater.avado.dnp.dappnode.eth:0.0.1'
    build:
      context: ./build
      args:
        TEKU_VERSION: 22.12.0
    volumes:
      - 'data:/data'
    environment:
      - JAVA_OPTS=-Xmx3g
      - EXTRA_OPTS=
volumes:
  data: {}
