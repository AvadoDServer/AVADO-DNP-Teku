version: '3.4'
services:
  teku.avado.dnp.dappnode.eth:
    image: 'teku.avado.dnp.dappnode.eth:0.0.35'
    build:
      context: ./build
      args:
        TEKU_VERSION: 23.5.0
    volumes:
      - 'data:/data'
    environment:
      - JAVA_OPTS=-Xmx3g
      - EXTRA_OPTS=
volumes:
  data: {}
