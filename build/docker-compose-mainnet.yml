version: '3.4'
services:
  teku.avado.dnp.dappnode.eth:
    image: 'teku.avado.dnp.dappnode.eth:0.0.44'
    build:
      context: ./build
      args:
        TEKU_VERSION: 23.12.0
    volumes:
      - 'data:/data'
    environment:
      - JAVA_OPTS=-Xmx8g
      - EXTRA_OPTS=
volumes:
  data: {}
